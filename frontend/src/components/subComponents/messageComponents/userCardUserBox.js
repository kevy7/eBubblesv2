import React, { Component } from 'react';

const userCardUserBox = (props) => {
    
    return (
        <div>
            <p>Display username here: {props.userName}</p>
            <hr />
        </div>
    );
};

export default userCardUserBox;